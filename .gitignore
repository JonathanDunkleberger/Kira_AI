# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Environment & secrets
.env
.env.*
!.env.example
*.token
.vts_token

# Data & models
memory_db/
memory_db.bak*/
models/
tools/stockfish/
logs/
cache/
*.gguf
*.log
*.wav
*.mp3
*.sqlite3
*.db

# Local-only state (chatter scores, per-game playthrough notes, lore w/ real names)
cookie_data.json
playthroughs/

# Local audio-device selection (machine-specific; created on first dashboard pick)
audio_device.json

# Local runtime data (lifetime chess stats, etc.) — gitignored, created on first run
data/

# Private persona files — real character prompt, identity anchors, opinions
persona/private/
# Stray root copy of the real persona — must never be committed (lives in persona/private/).
/personality.txt
lore/

# Personal content (streams, recordings, clips, transcripts)
# Anchored to repo root so it does NOT match the kira/clips/ source package.
/clips/
recordings/
transcripts/
*.mp4
*.mkv
*.mov
*.webm
*.flv

# Screenshots / captures — but keep the demo gif, architecture svg, overlay source assets, and generated scene PNGs
*.jpg
*.jpeg
*.png
!VTuber Demo - Kirav3.gif
!docs/kira-architecture.svg
!web_dashboard/assets/**
!web_dashboard/screens/*.png

# Storytime / Puppet Show: per-show generated scene art + manifests (ephemeral,
# regenerated each show — never committed).
/web_dashboard/storytime/

# Local-only scratch / editorial artifacts (not for the public repo)
repo_tree.txt
scripts/cuts_*.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/

# Virtual environments
venv/
.venv/

# Binaries
mpv.exe

# Copyright game ROMs — NEVER commit (FireRed etc.)
roms/
*.gba
*.gbc
*.gb

# Scratch / diagnostic throwaway scripts
tmp/

# runtime persistence (core Kira journey lives here; never in git)
states/

# Internal night-train ops docs — the train's own brain files. Kept on disk (never
# delete), but untracked from the public repo (Jonny-adjudicated 2026-07-15: internal
# operational churn + local paths + internal doctrine; exclude from public push).
/CLAUDE.md
/STATE_OF_PROJECT.md
/NIGHT_REPORT.md
/NEXT_SESSION.md
/NIGHT_REPORT_*.md
/NEXT_SESSION_*.md
