## MISSION: fresh_go_6 — THE CLEAN QUALIFYING CERTIFICATION (cold, zero-touch, bedroom -> credits)
Context: fresh_go_5 rolled credits 2026-07-15 11:01 = PIPELINE PROOF, but the attended mid-run terrain fix (1b85dcd) + resume put a human fingerprint on it. Jonny's adjudication: run 5 does NOT certify the asymptote. fresh_go_6 is the certification: the EXACT machine that just rolled credits, flown cold, ZERO human touches, ZERO code changes.

### ✅ ORDER 1 DONE — fresh_go_6 LAUNCHED COLD 2026-07-15 12:03:47 (shift 1)
- RUN_STATS_fresh_go_5.md: already committed at the CREDITS commit 22c5106 (credits-era file, 11 segments) — working tree was NOT ahead of HEAD, nothing to commit.
- Contamination guard EXECUTED: run-5 `banked_LIVE`, `banked_CREDITS`, `stage` renamed in place to `*_archived_fresh_go_5_final_0715` (KiraRunArchive\run5_first_credits copy also intact). Bare `banked_GOAL` does NOT exist (only prefixed variants) → carry-forward cannot resurrect run-5 state.
- Watchdog: `FRESH_GO_LOG=/g/temp/longrun/fresh_go_6.log nohup bash fresh_go_watchdog.sh` (identical to fresh_go_5 invocation), detached.
- **BOOT VERIFIED COLD in the log:** `boot=FRESH map=(0,0) badges=0 party=[]` → FRESH SPINE (the_opening -> ... -> Misty). Single-run law: exactly ONE watchdog tree (bash 21296→34448 → py 38672→36312, all 12:03:47).
- **Known benign cold-boot artifact (DO NOT TOUCH):** `!! soul seed failed: expected str, bytes ... not NoneType` fires once at FRESH boot because there is NO prior soul.json to seed from on a genuine cold start (fresh_go_5's log seeded from banked_LIVE/soul.json — that was the RESUMED segment, not a cold boot). Soul builds from play; `[soul] surface_want FIRE` / `ORACLE decision` fire normally immediately after. NOT a regression. Zero-touch: leave it.
- FLYING THE CURRENT COMMIT (22c5106 + 1b85dcd terrain fix) EXACTLY AS-IS. fix-(b) DEFERRED/FORBIDDEN mid-battery. Canonical `states/campaign/` UNTOUCHED.

### 🟢 LIVE MONITOR STATUS — shift 45 glance 2026-07-15 ~14:42 (HEALTHY + bounded — E4-gate OPEN-GRASS grind, roster ROTATED slot1→slot3: fresh motion, marks flat at 3 first glance; no touch)
- Watchdog tree: ONE alive, SAME segment as shifts 36-44 (top bash 21296 [12:03:47] → child bash 35788 [1:58:46] → py 38464 → py 27384 carry-forward). **0 tracebacks/errored whole-log.** Log mtime = now (14:42), **92.2k lines** (up from 88.9k, +3.3k all motion). **1580 battle wins** (up from 1488, +92 fresh). 0 new wedges. Confined to grind box map(3,20).
- **★ SHIFT-45 HALT-WINDOW DISCRIMINATOR (glance +1 after shift-44 reset) — VERDICT: NOT a halt, fresh motion via ROSTER ROTATION.** Fielded slot ROTATED **slot1(L33) → slot3(L33)** (grep `fielding slot`: last three = slot1 L32, slot1 L33, **slot3 L33**) — slot1 raticate reached L33 and the grind loop MOVED ON to field slot3 (kadabra) at L33 to work it up = the loop is rotating through the roster, not stuck on one slot. Battle wins +92 (1488→1580) = active battling. Marks FLAT at 3 this glance (first flat-marks glance) BUT slot rotation + win motion are the fresh signals → halt window does NOT advance.
- **un-grindable marks = 3 (flat vs shift 44: L28 lapras / L29 dugtrio / L32) — NOT yet a freeze.** The grind rotated from slot1 (now L33, at/near its cap) to slot3; a new mark would accrue when slot3 or another bench mon plateaus. Marks flat for ONE glance while the loop works a fresh slot = expected mid-rotation, not a livelock. WATCH: if marks stay 3 AND slot stays slot3/L33 for ~2 more glances with no escalate, that begins to mature the halt condition.
- **Why the gate line looks stuck at "stint 1/80":** the `E4-READINESS GATE RED (stint 1/80)` line fired ONCE at the badge-8 bank; the campaign then entered the GRIND-WEAK / open-grass sub-loop and stays inside it WITHOUT re-emitting the top gate line — it is EXECUTING stint 1's grind, not stalled re-printing it. Last gate line: `floor L26 < target L42 / gap 31 > 15; ace L57 CAPPED` = the ORIGINAL stint-start reading (stale, not live). "stint 1 across glances" is expected.
- **The 1b85dcd TERRAIN-ESCALATE to the VR cave has correctly NOT fired yet (0 matches whole-log, confirmed shift 45).** It triggers via `_floor_mon_stalled()` only when the CURRENT LOWEST mon is marked un-grindable with no grindable headroom left — but the pass is still actively fielding bench slots (now slot3 L33, not yet marked), so there's still open-grass headroom being worked. Once the remaining bench mons plateau+mark on open grass (everything <L38 marked), `_e4_gate_vr_escalate` fires → marches Viridian→R22→R23→VR→Indigo→VR2F cave (L40+ wilds) → floor climbs (fresh_go_5 verified 28→38). We are in the pre-escalation OPEN-GRASS phase; floor being worked up bottom-to-mid roster.
- Confinement to a ~4×4 box on map (3,20) = the OPEN-GRASS grind itself (pick nearby target → step-encounter → win → repeat), bounded, ace-led (capped), floor creeping. NOT a geographic wedge — grinding in place, as designed. TRAVEL WEDGE x4 at (10,79) self-recovers "returning to roam LOUD (no inner spin)" = the bounded 1:1 self-recover pattern, not a spin.
- **★ FORWARD PROGRESS (from shift 39):** Giovanni DEFEATED → **BADGE 8 (Earth) BANKED** at 703.0s (`levels=[57,31,29,33,30,26]`) → Viridian City → **ALL 8 BADGES** → E4-READINESS GATE fired on schedule. Endgame: Victory Road + Elite Four next.
- **SIX DISTINCT 0-dup team:** venusaur L57 (ace, CAPPED), raticate L33 (slot1, at/near cap), dugtrio L29, kadabra L33 (slot3, now being fielded), arbok L30, lapras (floor, marked). badges=8. Pokédex 12. Bridge INPUT OWNER='agent' sole writer. NO new wedges this glance.
- Nothing to touch. **THE ONLY frontier = E4-READINESS GATE convergence.** SUCCESSOR DECISIVE TEST: `grep TERRAIN-ESCALATE` + count `marking it un-grindable` + last `GRIND-WEAK: fielding slot` line. HEALTHY paths: (a) TERRAIN-ESCALATE appears → she marches to the VR cave → floor climbs L40+; OR (b) fielded slot advances/rotates (slot3 L33 → higher, or a new slot) / new un-grindable marks accrue (3→4) / battle wins keep climbing = floor still creeping on open grass. **ORDER 3 HALT trigger (window opened shift 41; RESET shift 44; +1 clean glance shift 45):** only if ~2 MORE glances (~30 min) pass with STILL no TERRAIN-ESCALATE, un-grindable marks frozen at 3, the fielded slot stuck (no advance/rotation past slot3/L33), NO fresh level motion, battle wins flat, and confined to map (3,20) — that = the open-grass marking loop never completing to fire the cave escalation (diagnosis: either `grind_weak_members` not returning to let `_e4_readiness_grind` call `_e4_gate_vr_escalate`, or the floor mon's per-encounter creep resets its stall-detection so it's never formally marked). Do NOT halt while ANY of {marks, fielded-slot rotation, wins} advancing (slot rotated slot1→slot3 + wins +92 this glance = they ARE).

ORDER 2 — MONITOR (all subsequent shifts): glance-clean, 0-fix 0-commit (monitor hygiene). Healthy = log growing, full core, 0 tracebacks, floors climbing at gates, gap holding/narrowing, NON-CONVERGING clusters bounded + self-recovering. Do NOT touch code, flags, canonical, or the run. Known accepted residual: ace-creep-despite-cap; fix-(b) is DEFERRED by Jonny — arming it mid-battery is FORBIDDEN.
**THE MONITOR GLANCE (run each shift):** (1) `Get-CimInstance Win32_Process` for bash+python watchdog tree = exactly ONE, alive; (2) `fresh_go_6.log` mtime = now + line count growing; (3) grep whole-log for `Traceback`/`errored` = 0; (4) tail: motion-not-spin (roam STATE advancing, spin markers are self-recovering `no inner spin`); (5) party = SIX DISTINCT 0-dup, badges climbing, ace-bench gap bounded; (6) at badge 8 → THE E4-READINESS GATE (RED → ace CAPPED → bench floor CLIMBING [+ terrain-escalate into VR2F cave if open grass starved] → gap≤15 all≥L42 GREEN → E4 → Champion → banked_CREDITS). If host-contention crawl (Jonny video-editing starves the emulator, ~1.75fps) — check host top-CPU BEFORE calling a wedge; a slow-but-growing log is NOT a wedge.

ORDER 3 — TERMINAL ADJUDICATION: if fresh NON-CONVERGING clusters resume AND the floor freezes below prior best across a multi-glance window / gap widening -> HALT line 1 of NIGHT_REPORT.md + one-page diagnosis (expected root: ace-creep; the fix is fix-(b) on a FRESH run = Jonny's call). Any other repeat-class failure -> HALT + diagnosis. Do NOT auto-relaunch a 7th run, ever.

ORDER 4 — ON CREDITS: the watchdog self-stops on the credits bank (banked_CREDITS appears) -> run `tools/run_stats.py G:/temp/longrun/fresh_go_6.log RUN_STATS_fresh_go_6.md` -> commit -> QUALIFYING EVAL against the FULL asymptote definition: fresh cold start; ZERO human touches whole-run; six DISTINCT species; all >= L42 at E4 entry; ace-floor gap <= 15 at the gate; no L100; bounded grind; stats generated; train self-stops. Brutally honest — ANY human touch anywhere disqualifies; NO asterisk this time. Write line 1: CREDITS + QUALIFYING (or CREDITS + explicit caveats) -> the loop stops.

PARKED — DO NOT TOUCH UNTIL THE VERDICT: fix-(b) ace-cap tightening; the missing-instrumentation list at the bottom of RUN_STATS_fresh_go_5.md; banked_CREDITS promotion to canonical (Jonny's call). Canonical saves UNTOUCHED, always.

---
(The full fresh_go_5 mountain survey — the machine that produced the qualifying shape — lives in git history at commit 22c5106 and in NIGHT_REPORT.md; not re-copied here to keep this resume prompt tight.)
