torch==2.9.1
transformers
SpeechRecognition
pyaudio
numpy
# NOTE: llama-cpp-python is NOT installed here. It only powers the OPTIONAL
# local-Llama inference backend (INFERENCE_BACKEND=local), and it must compile
# against a C++ toolchain (+ CUDA for GPU), which trips up most fresh clones.
# The default groq backend needs none of it. If — and only if — you want the
# local fallback, run:  pip install -r requirements-local.txt
# For advanced VAD and high-quality TTS
webrtcvad-wheels
elevenlabs
# For Dashboard UI
Pillow
# For web search
google-api-python-client
# For advanced memory (vector database)
chromadb==1.0.15
sentence-transformers
faster-whisper
twitchio==2.10.0
yt-dlp
python-dotenv
pygame
edge-tts
azure-cognitiveservices-speech
# For VN auto-play (keyboard/mouse control)
pyautogui
pydirectinput
# For audio understanding (WASAPI loopback system audio + mic capture)
PyAudioWPatch
# For deep cognitive moments (Claude Opus routing)
anthropic==0.102.0
# For cloud Llama-3.1-8b triage/tool inference (VRAM-free)
groq==1.4.0
# For global hotkeys (F8/F9 interrupt + mute)
keyboard
# For YouTube live chat
pytchat
# For local VN text OCR (eliminates cloud vision latency for text reading)
# NOTE: also requires the Tesseract binary: https://github.com/UB-Mannheim/tesseract/wiki
pytesseract
# Fish Audio TTS SDK (pay-as-you-go streaming TTS backend, switchable vs Azure)
fish-audio-sdk
# Chess Mode (Phase 1): Lichess bot client + board model / UCI engine driver.
# Also needs a local Stockfish binary (CHESS_ENGINE_PATH) — install separately.
berserk
chess
# Whole-card VRAM telemetry for the dashboard (NVML). Provides the `pynvml`
# import; reports the entire GPU's used/total so headroom is visible during
# AAA sessions (torch's allocator only sees Kira's own footprint).
nvidia-ml-py
# Storytime / Puppet Show image generation (Google Gemini 2.5 Flash Image,
# "Nano-Banana"). Provider-agnostic client; swap out without touching the show.
google-genai
